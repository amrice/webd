name: Docker Build & Push

on:
  push:
    branches: [master, main]
    paths:
      - 'Dockerfile'
      - '.github/workflows/docker-build.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download webd binary from official site
        run: |
          URL="https://webd.cf/webd/webd_dl/20240223/webd-20240223-x86_64-linux-musl.tar.gz"
          echo ">>> $URL"
          curl -fsSL -H "User-Agent: Mozilla/5.0" "$URL" -o /tmp/webd.tar.gz
          tar -xzf /tmp/webd.tar.gz -C /tmp
          cp /tmp/webd/webd ./webd-bin
          chmod +x webd-bin
          ls -lh webd-bin

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/webd:20240223
            ghcr.io/${{ github.repository_owner }}/webd:latest
